---
description: 提交、推送和发布时禁止带上 Cursor 署名
alwaysApply: true
---

# 禁止 Cursor 署名

GitHub 提交、推送、标签和 Release 只署名仓库所有者，不要出现 Cursor / Cursor Agent。

- 提交作者与提交者使用 `Luomin <39723210+tianyugithub@users.noreply.github.com>`
- 不要改动本机 `git config`
- 提交说明里不要写 `Co-authored-by: Cursor`、`Made-with: Cursor` 或任何 Cursor Agent 邮箱
- 不要把 Cursor 加进 Release 发布人、贡献者或提交记录
- 推送前检查 `git log -1 --format='%an %ae%n%b'`，若含 Cursor 字样则去掉后再推
